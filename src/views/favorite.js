import * as React from 'react';
import {Text, View} from 'react-native';

function FavoriteView() {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Favori Kelimelerim</Text>
    </View>
  );
}

export default FavoriteView;
